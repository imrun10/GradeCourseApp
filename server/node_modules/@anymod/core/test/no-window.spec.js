describe('SSR', () => {
  it('should import core without window object', async () => {
    delete global.window;
    const core = require("../src/index.js");
    await core.default.render()
    expect('This test to pass if code above did not error').toBeTruthy();
  });
});